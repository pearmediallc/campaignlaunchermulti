# Production Environment Variables Template for Render
# Copy this to Render's environment variables section

NODE_ENV=production
PORT=10000

# Database Configuration (You'll need to set up a database)
# Option 1: Use Render PostgreSQL (free tier available)
# Option 2: Use external MySQL service like PlanetScale, AWS RDS, or DigitalOcean
DB_HOST=your-database-host.com
DB_PORT=3306
DB_NAME=fb_campaign_launcher
DB_USER=your-db-username
DB_PASSWORD=your-db-password

# Redis Configuration (Optional - for session management)
# You can use Render Redis or external service like Upstash
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# Security Keys (MUST CHANGE THESE!)
JWT_SECRET=generate-a-long-random-string-here-at-least-32-chars
SESSION_SECRET=another-long-random-string-for-sessions
ENCRYPTION_KEY=64-character-hex-string-for-token-encryption

# Facebook App Configuration
FB_APP_ID=735375959485927
FB_APP_SECRET=your-facebook-app-secret-from-dashboard
FB_API_VERSION=v18.0
FB_REDIRECT_URI=https://your-app-name.onrender.com/api/auth/facebook/callback

# Application URLs (Update after Render gives you URL)
FRONTEND_URL=https://your-app-name.onrender.com
BACKEND_URL=https://your-app-name.onrender.com

# Email Configuration (if needed for notifications)
EMAIL_FROM=noreply@yourdomain.com
SUPPORT_EMAIL=support@yourdomain.com